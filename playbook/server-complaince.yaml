---
- name: Configure Servers
  hosts: servers
  become: true
  gather_facts: true

  tasks:
    - name: Ensure APT package cache is updated
      ansible.builtin.apt:
        update_cache: true
        cache_valid_time: 3600